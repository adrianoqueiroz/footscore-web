# Configuração Local - IP da Rede
# Copie este arquivo para env.local e ajuste o IP conforme necessário
# Este arquivo não deve ser commitado no git

# IP da sua máquina na rede local (para acesso do celular)
# Exemplo: 192.168.0.16
LOCAL_NETWORK_IP=192.168.0.16

# Porta do backend (geralmente 3000)
BACKEND_PORT=3000

# Se você quiser usar localhost ao invés do IP (para desenvolvimento no computador)
# Descomente a linha abaixo e comente a VITE_API_BASE_URL com IP
# VITE_API_BASE_URL=http://localhost:3000/api

# URL da API usando o IP da rede local (para acesso do celular)
VITE_API_BASE_URL=http://192.168.0.16:3000/api
