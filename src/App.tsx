import AppRoutes from './router'

function App() {
  // Renderizar o app normalmente sem verificações automáticas de conexão
  // A tela de erro será mostrada apenas quando houver falhas em ações específicas do usuário
  return <AppRoutes />
}

export default App

